# Plataforma de Cursos Online

Projeto em React + TypeScript com Bootstrap, roteamento e consumo de API via JSON Server.

## Como executar

1. Instale as dependências:
   `npm install`

2. Inicie a API local:
   `npm run server`

3. Inicie o front-end:
   `npm run dev`
